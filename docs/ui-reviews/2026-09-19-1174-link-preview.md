# 2026-09-19 公開投稿のURL linkとOGP preview

- Status: current
- Supersedes: None
- Superseded by: None
- PR: Issue #1174の実装PR（`Refs #1174`）
- Preview: Issue添付のbefore画像、Storybook `Core/PostCard/ExternalLinkPreview`、PRのafter条件
- Surface / user / purpose: Timeline、Profile、Bookmarks、Thread、Community Indexで公開投稿を読む利用者。投稿内URLを識別・操作でき、先頭URLの概要を投稿文脈内で確認できるようにする。
- Summary: 全URLをinline link化し、eligibleな公開投稿の先頭URLだけにboundedなOGP cardを追加する。取得不能・非対象状態はlink-only。private／非表示contentを自動取得しない。
- Conditions:
  - Platform: Chromium browser test、Windows WebView2実機
  - Viewport: narrow Column、standard、wide、200% zoom
  - Theme: dark／light
  - Locale: ja／en／zh-CN
  - State: text-only、imageあり／なし、長文、取得失敗、private、adult gate、trust collapse、pending、offscreen
- Accessibility / interaction: inline linkとcardを通常Tabで個別にfocusでき、light／darkのvisible focusと親thread actionの非発火を確認した。preview imageは空alt／`aria-hidden`の装飾扱い、cardのaccessible nameはtitleとsiteを持つ。component testでSVG dataを画像として描画しないことも固定した。
- Performance: viewport交差後に先頭1件だけ取得し、同時4件、in-flight dedupe、128 entry／16 MiBのprocess cacheを使う。skeletonを置かずtimeline renderを待たせない。
- Validation: `cargo xtask desktop-ui-check`（Vitest 1,901件、Storybook build、Chromium 366件、visual 42件）PASS。IntersectionObserverのtarget／rootMargin修正後は`LinkPreviewCard.test.tsx`／`PostCard.test.tsx`／Community Index test 37件、typecheck、lint、Storybook buildを再実行してPASS。Windows上のStorybook実描画でdark／light、狭幅、長いtitle／description、imageあり、inline link／cardのfocusを確認した。Linux baselineは最終workflow run `35411041556`でmissing状態から再生成し、同意画面2枚のbadge／施行日／変更要約をlegal version 8へ一致させた。
- Not verified: packaged Windows Tauriから実在public URLを取得する実通信とOS browser起動の組合せ。Tauri test executableは既存DLLのentrypoint不整合で、既存target／専用のfresh targetの両方でtest開始前に終了した。`cargo xtask tauri-check`とCIでcompile／platform差を補完し、外部browser単体は既存command testを維持する。
- Review result: PASS（上記native unit実行不能はPRの未確認事項として保持し、CI failure時はmergeしない）
- Exceptions: None
