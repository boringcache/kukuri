# v0.2.7-preview.1 リリース

## 現在の状態

- クライアント5本体（Windows NSIS、Linux AppImage／Deb、CLI x86_64／aarch64）を source `e820382d8`（tag `v0.2.7-preview.1`）から公開した。Community Node の image 更新・VM 反映は含めない。
- 目的は Windows のクリーンインストールで「persisted keyring identity is unavailable」により起動できない不具合の修正（[#1163](https://github.com/kukuri-app/kukuri/pull/1163)）を配布すること。修正版へ更新すれば、既に起動不能になった環境も再インストールなしで復旧する。
- 受入条件は v0.2.6-preview.1 と同じ: 同一sourceの5本体、3 updater entryの実署名・改変拒否、公開後取得照合。

## sourceと検証

- 版同期は区分Aの機械的保守。workspace・Tauri・frontendを0.2.7へ同期。Cargo.lockは自前packagesのversionだけが変わり、外部依存と依存関係は不変。
- `cargo xtask release-check v0.2.7-preview.1`、`git diff --check` 成功。
- 準備PR [#1164](https://github.com/kukuri-app/kukuri/pull/1164) は、差分が版番号と作業記録だけのため、運営者の指示で例外的にCI完了前にマージした。マージ後に同PRの15 checksは全て成功。

## クライアント公開

- [v0.2.7-preview.1](https://github.com/kukuri-app/kukuri/releases/tag/v0.2.7-preview.1) を 2026-09-18 10:22:35 UTC（19:22 JST）に公開。Latest、draft=false、既存updater互換のprerelease=false。assetsは21件。
- 公開run [35321581063](https://github.com/kukuri-app/kukuri/actions/runs/35321581063) の初回は `linux-verify` の Desktop Vitest で2件が負荷下のtimeoutで失敗した（`DesktopShellPage.bookmarksEmptyState.test.tsx` の読込中表示の取りこぼし、`DesktopShellPage.profile.test.tsx` の10秒timeout）。差分と無関係で、資材生成前の失敗のため同一runの失敗jobだけを再実行し、全job成功（linux-verify、CLI 2arch、Linux package、Windows package、changelog、release-assets、publish-draft、verify-published）。
- `latest-preview.json` は version 0.2.7、3 entry とも当該tagのURLと非空signatureを持つ。移管前の既存clientの更新先（`KingYoSun/kukuri` の安定URL）からも redirect で同じ manifest を取得できる。
- tag pushのdraft run `35321525726` と、同一commitの重複するmain CN run `35321459841` は取消した。tag起点のCN image run `35321524434` は成功（imageの公開のみで、VMへは反映していない）。
- `changelog` job は `chore/changelog-v0.2.7-preview.1`（tag起点）を push したがPRを作成しなかった（v0.2.6と同じ）。main への取込みは未実施。
