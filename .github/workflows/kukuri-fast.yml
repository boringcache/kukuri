name: Kukuri Fast

on:
  pull_request:
    paths:
      - '.config/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain.toml'
      - '.cargo/**'
      - 'xtask/**'
      - 'crates/**'
      - 'apps/**'
      - 'harness/**'
      - '.github/workflows/kukuri-fast.yml'
      - '.github/workflows/kukuri-nightly.yml'
      - '.github/workflows/kukuri-release.yml'
  push:
    branches: [main, develop]
    paths:
      - '.config/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain.toml'
      - '.cargo/**'
      - 'xtask/**'
      - 'crates/**'
      - 'apps/**'
      - 'harness/**'
      - '.github/workflows/kukuri-fast.yml'
      - '.github/workflows/kukuri-nightly.yml'
      - '.github/workflows/kukuri-release.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  linux-rust-static:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # BoringCache identifies this repository through GitHub OIDC
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.92
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.92.0
          components: clippy,rustfmt

      - name: Install Linux system dependencies
        run: >
          sudo apt-get update &&
          sudo apt-get install -y
          pkg-config
          libdbus-1-dev
          libglib2.0-dev
          libgtk-3-dev
          libwebkit2gtk-4.1-dev
          libayatana-appindicator3-dev
          librsvg2-dev
          patchelf

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Cache Rust with BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          trust-policy: auto
          mode: cargo
          cache-profiles: linux-rust-static
          diagnostics: summary

      - name: Doctor
        run: cargo xtask doctor

      - name: Oversized file report
        run: cargo xtask oversized-files

      - name: Asset rights manifest
        run: cargo xtask asset-check

      - name: Rust checks
        run: cargo xtask rust-check

      - name: IPC types are in sync (Rust -> types.generated.ts)
        run: cargo xtask ipc-types --check

      - name: Tauri compile check
        run: cargo xtask tauri-check

      - name: Show sccache stats
        if: always()
        shell: bash
        run: |
          if [ -n "${SCCACHE_PATH:-}" ]; then
            "$SCCACHE_PATH" --show-stats || true
          elif command -v sccache >/dev/null 2>&1; then
            sccache --show-stats || true
          fi

  linux-rust-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # BoringCache identifies this repository through GitHub OIDC
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.92
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.92.0

      - name: Install Linux system dependencies
        run: >
          sudo apt-get update &&
          sudo apt-get install -y
          pkg-config
          libdbus-1-dev
          libglib2.0-dev
          libgtk-3-dev
          libwebkit2gtk-4.1-dev
          libayatana-appindicator3-dev
          librsvg2-dev
          patchelf

      - name: Cache Rust with BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          trust-policy: auto
          mode: cargo
          cache-profiles: linux-rust-tests
          diagnostics: summary

      - name: Install nextest
        uses: taiki-e/install-action@nextest

      - name: Rust tests
        run: cargo xtask rust-test

      - name: Show sccache stats
        if: always()
        shell: bash
        run: |
          if [ -n "${SCCACHE_PATH:-}" ]; then
            "$SCCACHE_PATH" --show-stats || true
          elif command -v sccache >/dev/null 2>&1; then
            sccache --show-stats || true
          fi

  linux-desktop-ui:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # BoringCache identifies this repository through GitHub OIDC
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.92
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.92.0

      - name: Install Linux system dependencies
        run: >
          sudo apt-get update &&
          sudo apt-get install -y
          pkg-config
          libdbus-1-dev
          libglib2.0-dev
          libgtk-3-dev
          libwebkit2gtk-4.1-dev
          libayatana-appindicator3-dev
          librsvg2-dev
          patchelf

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.16.1
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: apps/desktop/pnpm-lock.yaml

      - name: Cache Rust with BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          trust-policy: auto
          mode: cargo
          cache-profiles: linux-desktop-ui
          diagnostics: summary

      - name: Install desktop dependencies
        working-directory: apps/desktop
        run: pnpm install --frozen-lockfile

      - name: Desktop lint + typecheck
        run: cargo xtask desktop-lint

      - name: Desktop Vitest
        run: cargo xtask desktop-test

      - name: Show sccache stats
        if: always()
        shell: bash
        run: |
          if [ -n "${SCCACHE_PATH:-}" ]; then
            "$SCCACHE_PATH" --show-stats || true
          elif command -v sccache >/dev/null 2>&1; then
            sccache --show-stats || true
          fi

  linux-desktop-browser:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # BoringCache identifies this repository through GitHub OIDC
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.92
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.92.0

      - name: Install Linux system dependencies
        run: >
          sudo apt-get update &&
          sudo apt-get install -y
          pkg-config
          libdbus-1-dev
          libglib2.0-dev
          libgtk-3-dev
          libwebkit2gtk-4.1-dev
          libayatana-appindicator3-dev
          librsvg2-dev
          patchelf
          fonts-noto-cjk

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.16.1
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: apps/desktop/pnpm-lock.yaml

      - name: Cache Rust with BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          trust-policy: auto
          mode: cargo
          cache-profiles: linux-desktop-browser
          diagnostics: summary

      - name: Install desktop dependencies
        working-directory: apps/desktop
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Chromium
        working-directory: apps/desktop
        run: pnpm exec playwright install chromium

      - name: Desktop Storybook build
        run: cargo xtask desktop-storybook

      - name: Desktop browser tests
        run: cargo xtask desktop-browser-test

      - name: Desktop visual regression tests
        run: cargo xtask desktop-visual-test

      - name: Upload visual regression diff
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kukuri-desktop-visual-diff
          path: apps/desktop/test-results/
          if-no-files-found: ignore

      - name: Show sccache stats
        if: always()
        shell: bash
        run: |
          if [ -n "${SCCACHE_PATH:-}" ]; then
            "$SCCACHE_PATH" --show-stats || true
          elif command -v sccache >/dev/null 2>&1; then
            sccache --show-stats || true
          fi

  linux-cn:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # BoringCache identifies this repository through GitHub OIDC
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.92
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.92.0

      - name: Install Linux system dependencies
        run: >
          sudo apt-get update &&
          sudo apt-get install -y
          pkg-config
          ffmpeg
          libdbus-1-dev
          libglib2.0-dev
          libgtk-3-dev
          libwebkit2gtk-4.1-dev
          libayatana-appindicator3-dev
          librsvg2-dev
          patchelf

      - name: Cache Rust with BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          trust-policy: auto
          mode: cargo
          cache-profiles: linux-cn
          diagnostics: summary

      - name: Community-node checks
        run: cargo xtask cn-check

      - name: Community-node integration tests
        run: cargo xtask cn-test

      - name: Show sccache stats
        if: always()
        shell: bash
        run: |
          if [ -n "${SCCACHE_PATH:-}" ]; then
            "$SCCACHE_PATH" --show-stats || true
          elif command -v sccache >/dev/null 2>&1; then
            sccache --show-stats || true
          fi

  linux-cn-e2e:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # BoringCache identifies this repository through GitHub OIDC
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.92
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.92.0

      - name: Install Linux system dependencies
        run: >
          sudo apt-get update &&
          sudo apt-get install -y
          pkg-config
          libdbus-1-dev
          libglib2.0-dev
          libgtk-3-dev
          libwebkit2gtk-4.1-dev
          libayatana-appindicator3-dev
          librsvg2-dev
          patchelf

      - name: Cache Rust with BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          trust-policy: auto
          mode: cargo
          cache-profiles: linux-cn-e2e
          diagnostics: summary

      - name: Community-node full-stack E2E
        run: cargo xtask cn-e2e

      - name: Show sccache stats
        if: always()
        shell: bash
        run: |
          if [ -n "${SCCACHE_PATH:-}" ]; then
            "$SCCACHE_PATH" --show-stats || true
          elif command -v sccache >/dev/null 2>&1; then
            sccache --show-stats || true
          fi

  linux-smoke:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # BoringCache identifies this repository through GitHub OIDC
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.92
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.92.0

      - name: Install Linux system dependencies
        run: >
          sudo apt-get update &&
          sudo apt-get install -y
          pkg-config
          libdbus-1-dev
          libglib2.0-dev
          libgtk-3-dev
          libwebkit2gtk-4.1-dev
          libayatana-appindicator3-dev
          librsvg2-dev
          patchelf

      - name: Cache Rust with BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          trust-policy: auto
          mode: cargo
          cache-profiles: linux-smoke
          diagnostics: summary

      - name: E2E smoke
        run: cargo xtask e2e-smoke

      - name: Upload smoke artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kukuri-fast-smoke-artifacts
          path: test-results/kukuri/
          if-no-files-found: warn

      - name: Show sccache stats
        if: always()
        shell: bash
        run: |
          if [ -n "${SCCACHE_PATH:-}" ]; then
            "$SCCACHE_PATH" --show-stats || true
          elif command -v sccache >/dev/null 2>&1; then
            sccache --show-stats || true
          fi

  linux-community-node:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # BoringCache identifies this repository through GitHub OIDC
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.92
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.92.0

      - name: Install Linux system dependencies
        run: >
          sudo apt-get update &&
          sudo apt-get install -y
          pkg-config
          libdbus-1-dev
          libglib2.0-dev
          libgtk-3-dev
          libwebkit2gtk-4.1-dev
          libayatana-appindicator3-dev
          librsvg2-dev
          patchelf

      - name: Cache Rust with BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          trust-policy: auto
          mode: cargo
          cache-profiles: linux-community-node
          diagnostics: summary

      - name: Community-node connectivity scenario
        env:
          CN_POSTGRES_PORT: "55433"
          COMMUNITY_NODE_DATABASE_URL: postgres://cn:cn_password@127.0.0.1:55433/cn
        run: cargo xtask scenario community_node_public_connectivity

      - name: Community-node index client scenario
        env:
          KUKURI_DISABLE_KEYRING: "1"
        run: |
          cargo xtask scenario community_node_index_query_client
          cargo xtask scenario community_node_report_routing
          cargo xtask scenario community_node_trust_relation_client

      - name: Upload community-node artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kukuri-fast-community-node-artifacts
          path: test-results/kukuri/
          if-no-files-found: warn

      - name: Show sccache stats
        if: always()
        shell: bash
        run: |
          if [ -n "${SCCACHE_PATH:-}" ]; then
            "$SCCACHE_PATH" --show-stats || true
          elif command -v sccache >/dev/null 2>&1; then
            sccache --show-stats || true
          fi

  windows-fast:
    runs-on: windows-latest
    permissions:
      contents: read
      id-token: write  # BoringCache identifies this repository through GitHub OIDC
    timeout-minutes: 75
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.92
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.92.0
          components: clippy,rustfmt

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.16.1
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: apps/desktop/pnpm-lock.yaml

      - name: Cache Rust with BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          trust-policy: auto
          mode: cargo
          cache-profiles: windows-fast
          diagnostics: summary
          # #1063: keep the dependency build a timed-out attempt reached.
          save-always: true

      - name: Install desktop dependencies
        working-directory: apps/desktop
        run: pnpm install --frozen-lockfile

      - name: Doctor
        run: cargo xtask doctor

      - name: Oversized file report
        run: cargo xtask oversized-files

      - name: Windows Tauri compile check
        timeout-minutes: 20
        run: cargo xtask tauri-check

      - name: E2E smoke
        timeout-minutes: 15
        run: cargo xtask e2e-smoke

      - name: Windows package
        # #1063: cache なしの実測 23〜25 分に対し、70% が 31.5 分となる値。
        timeout-minutes: 45
        run: cargo xtask desktop-package

      - name: Upload kukuri artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kukuri-windows-fast-artifacts
          path: test-results/kukuri/
          if-no-files-found: warn

      - name: Upload NSIS installer
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kukuri-windows-nsis
          path: apps/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/
          if-no-files-found: warn

      - name: Show sccache stats
        if: always()
        shell: bash
        run: |
          if [ -n "${SCCACHE_PATH:-}" ]; then
            "$SCCACHE_PATH" --show-stats || true
          elif command -v sccache >/dev/null 2>&1; then
            sccache --show-stats || true
          fi
