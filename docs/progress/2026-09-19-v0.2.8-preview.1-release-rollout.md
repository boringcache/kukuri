# v0.2.8 リリース（v0.2.8-preview.1 失敗 → v0.2.8-preview.2 公開）

## 現在の状態

- クライアント5本体（Windows NSIS、Linux AppImage／Deb、CLI x86_64／aarch64）を source `a9536dfd`（tag `v0.2.8-preview.2`）から公開した。Community Node の image 更新・VM 反映は含めない。
- 目的は v0.2.7-preview.1 以降の main の変更（URL パースと OGP プレビュー #1179、カスタムリアクション名の tooltip #1178、composer への画像の貼り付け #1177、画像 viewer のクリッピング修正 #1175 など）を配布し、あわせて Namespace へ移した release workflow（[#1180](https://github.com/kukuri-app/kukuri/issues/1180)）を実際の release で確認すること。実 release での確認はユーザー承認済み（2026-09-19）。
- 受入条件は v0.2.7-preview.1 と同じ: 同一sourceの5本体、3 updater entryの実署名・改変拒否、公開後取得照合。

## sourceと検証

- 版同期は区分Aの機械的保守（[#1182](https://github.com/kukuri-app/kukuri/pull/1182)）。workspace・Tauri・frontendを0.2.8へ同期。Cargo.lockは自前packagesのversionだけが変わり、外部依存と依存関係は不変。
- `cargo xtask release-check v0.2.8-preview.1`／`v0.2.8-preview.2`、`git diff --check` 成功。

## v0.2.8-preview.1（失敗、未公開）

- tag `v0.2.8-preview.1`（source `e720649d`）の公開 run [35415877964](https://github.com/kukuri-app/kukuri/actions/runs/35415877964) は、linux-verify が Namespace の Ubuntu 22.04 image に PowerShell が無いことで失敗した。publish 系は skipped で、Release は作成されていない。
- 修正（[#1183](https://github.com/kukuri-app/kukuri/pull/1183): PowerShell の導入、linux-verify の reusable workflow 化）の後、tag を上書きせず同じ 0.2.8 を `v0.2.8-preview.2` として公開した。tag `v0.2.8-preview.1` は Release の無い tag として残る。

## クライアント公開

- [v0.2.8-preview.2](https://github.com/kukuri-app/kukuri/releases/tag/v0.2.8-preview.2) を 2026-09-19 03:51:39 UTC（12:51 JST）に公開。Latest、draft=false、既存updater互換のprerelease=false。assetsは21件。
- 公開run [35418444116](https://github.com/kukuri-app/kukuri/actions/runs/35418444116) は全job成功（validate、linux-verify、CLI 2arch、Linux package、Windows package、changelog、release-assets、publish-draft、verify-published）。全体 28 分 25 秒。job 別の所要は [#1180 の作業記録](./2026-09-19-1180-release-namespace.md)。
- `latest-preview.json` は version 0.2.8、3 entry とも当該tagのURLと非空signatureを持つ。
- tag pushのdraft run `35418430413` は取消した。
- `changelog` job は `chore/changelog-v0.2.8-preview.2`（tag起点）を push したがPRを作成しなかった（v0.2.7と同じ）。さらに、直前の tag を自動で選ぶため、未公開の `v0.2.8-preview.1` を起点にしてしまい、#1183 の 1 件だけの節になった。公開済み Release の本文の「Changes」も同じ 1 件だけになった。根本の修正は [#1186](https://github.com/kukuri-app/kukuri/issues/1186)。
- CHANGELOG は `update-changelog.ps1` に `-PreviousTag` を明示して作り直し、main へ取り込んだ: `v0.2.7-preview.1`（`v0.2.6-preview.1..v0.2.7-preview.1`、main へ未取込みだった分）と `v0.2.8-preview.2`（`v0.2.7-preview.1..v0.2.8-preview.2`）。
- LP（`apps/lp/release.json`）を v0.2.8-preview.2 に更新し、ダウンロードのリンク 6 件がすべて 200 を返すことを確かめた。brief は v7。
- 2026-09-19、操作者の承認を得て、公開済み Release の本文の「Changes」を CHANGELOG と同じ節（起点 `v0.2.7-preview.1`）に差し替えた。assets は変えていない。release job が作った branch `chore/changelog-v0.2.8-preview.2` は不要になったため削除した。
